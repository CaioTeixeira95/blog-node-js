{{#each erros}}
  <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h4>Nova Categoria: </h4>
<div class="card ">
  <div class="card-body">
    <form class="" action="/admin/categorias/nova" method="post">
      <label for="nome">Nome: </label><br>
      <input type="text" name="nome" id="nome" placeholder="Nome da categoria" class="form-control"><br><br>
      <label for="slug">Slug: </label><br>
      <input type="text" name="slug" id="slug" placeholder="Slug da categoria" class="form-control"><br><br>
      <button type="submit" name="button" class="btn btn-primary">Criar Categoria</button>
    </form>
  </div>
</div>
